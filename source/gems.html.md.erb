# Gems

| Name | Used directly | Used indirectly |
| --- | --- | --- |
<% Dependency.all.each do |gem| %>
| <%= link_to gem.name, "/gems/#{gem.name}.html" %> | <%= gem.depended_on_directly.size %> | <%= gem.depended_on_indirectly.size %> |
<% end %>
