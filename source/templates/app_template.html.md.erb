# <%= app.name %>

- <%= link_to "Docs", app.api_data.dig("links", "html_url") %>
- <%= link_to "GitHub", app.api_data.dig("links", "repo_url") %>
- <%= link_to "Gemfile", app.api_data.dig("links", "repo_url") + "/blob/master/Gemfile" %>

## Vulnerabilities

| Gem | Vulnerable version | Summary | Patched versions |
| --- | --- | --- | --- |
<% app.vulnerabilities.each do |vuln| %>
| <%= link_to vuln['name'], "/gems/#{vuln['name']}.html" %> | <%= vuln['version'] %> | <%= link_to vuln['advisory'], vuln['url'] %>: <%= vuln['title'].gsub("\n", '<br />') %><br /><%= vuln['description'].gsub("\n", '<br />') %> | <%= vuln['patched_versions'].join('<br />') %> |
<% end %>

## Direct dependencies

| Name | Number of apps that use this |
| --- | --- |
<% app.direct_dependencies.each do |gem| %>
| <%= link_to gem.name, "/gems/#{gem.name}.html" %> | <%= gem.depended_on.size %> |
<% end %>

## Einzelgems

| Name |
| --- |
<% app.einzelgems.each do |gem| %>
| <%= link_to gem.name, "/gems/#{gem.name}.html" %> |
<% end %>
