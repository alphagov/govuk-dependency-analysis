# <%= app.name %>

## Direct dependencies

| Name | Usage count |
| --- | --- |
<% app.direct_dependencies.each do |gem| %>
| <%= link_to gem.name, "/gems/#{gem.name}.html" %> | <%= gem.depended_on.size %> |
<% end %>

## Einzelgems

| Name |
| --- |
<% app.einzelgems.each do |gem| %>
| <%= link_to gem.name, "/gems/#{gem.name}.html" %> |
<% end %>
