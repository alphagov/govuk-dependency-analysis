# <%= dep.name %>

- <%= link_to "rubygems.org/gems/#{dep.name}", "https://rubygems.org/gems/#{dep.name}" %>

### Applications that depend on this gem

| Name | Version |
| --- | --- |
<% dep.depended_on.each do |app| %>
| <%= link_to app.name, "/apps/#{app.name}.html" %> | <%= BaseData.get["versions"][app.name][dep.name] %> |
<% end %>

### Applications that don't depend on this

| Name |
| --- |
<% dep.apps_not_using_it.each do |app| %>
| <%= link_to app.name, "/apps/#{app.name}.html" %> |
<% end %>
