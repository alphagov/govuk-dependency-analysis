# <%= gem.name %>

- <%= link_to "rubygems.org/gems/#{gem.name}", "https://rubygems.org/gems/#{gem.name}" %>

### Applications that depend on this gem

| Name | Version |
| --- | --- |
<% gem.depended_on.each do |app| %>
| <%= link_to app.name, "/apps/#{app.name}.html" %> | <%= BaseData.get["versions"][app.name][gem.name] %> |
<% end %>

### Applications that don't depend on this

| Name |
| --- |
<% gem.apps_not_using_it.each do |app| %>
| <%= link_to app.name, "/apps/#{app.name}.html" %> |
<% end %>
