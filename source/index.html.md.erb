---
title: GOV.UK dependency explorer
---

# GOV.UK dependency explorer

We run **<%= stats[:application_count] %> Ruby applications**.
These depend on [<%= stats[:dependency_count] %> gems](/gems.html).
<%= stats[:einzelgems] %> of those are only used by one application ("einzelgems").

| Name | Direct dependencies | Transitive dependencies | Einzelgems |
| --- | --- | --- | --- |
<% Application.all.sort_by(&:transitive_dependency_count).reverse.each do |app| %>
| <%= link_to app.name, "/apps/#{app.name}.html" %> | <%= app.direct_dependency_count %> | <%= app.transitive_dependency_count %> | <%= app.einzelgem_count %> |
<% end %>
