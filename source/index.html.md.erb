---
title: GOV.UK dependencies
---

# GOV.UK dependencies

## Applications

We run **<%= stats[:application_count] %> Ruby applications**.
These depend on <%= stats[:dependency_count] %> gems.
<%= stats[:einzelgems] %> of those are only used by one application.

| Name | Number of dependencies | Transitive dependencies | Einzelgems |
| --- | --- | --- | --- |
<% Application.all.sort_by(&:transitive_dependency_count).reverse.each do |app| %>
| <%= link_to app.name, "##{app.name}" %> | <%= app.direct_dependency_count %> | <%= app.transitive_dependency_count %> | <%= app.einzelgem_count %> |
<% end %>

<% Application.all.each do |app| %>
### <%= app.name %>

#### Direct dependencies

| Name | Usage count |
| --- | --- |
<% app.direct_dependencies.each do |gem| %>
| <%= link_to gem.name, "/gems.html##{gem.name}" %> | <%= gem.depended_on.size %> |
<% end %>

#### Einzelgems

| Name |
| --- |
<% app.einzelgems.each do |gem| %>
| <%= gem.name %> |
<% end %>
<% end %>
